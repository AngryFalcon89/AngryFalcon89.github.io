{
  "name": "ahmad-bilal-portfolio",
  "version": "2.0.0",
  "description": "Ahmad Bilal Zaidi — Luxury Minimalist Software Engineer Portfolio",
  "scripts": {
    "build:css": "npx @tailwindcss/cli -i ./src/input.css -o ./styles.css --minify",
    "dev:css": "npx @tailwindcss/cli -i ./src/input.css -o ./styles.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.0.0",
    "tailwindcss": "^4.0.0"
  }
}
